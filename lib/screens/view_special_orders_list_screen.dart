import 'package:flutter/material.dart';

class ViewSpecialOrdersList extends StatelessWidget {
  const ViewSpecialOrdersList({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ViewSpecialOrdersList'),
      ),
      body: Center(
        child: Text('ViewSpecialOrdersList'),
      ),
    );
  }
}
